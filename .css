* {
  box-sizing: border-box;
  font-family: 'Press Start 2P', monospace;
  image-rendering: pixelated;
}

body {
  margin: 0;
  padding: 24px;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  
  /* =======================================================
     COLOQUE O FUNDO AQUI:
     Substitua 'coloque-sua-imagem-aqui.png' pelo caminho da sua imagem.
     ======================================================= */
  background-color: #d8b4f8;
  background-image: url('coloque-sua-imagem-aqui.png');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

.pixel-card {
  background-color: #f2e9fc;
  border: 4px solid #4a2069;
  box-shadow: 6px 6px 0px #4a2069;
  width: 100%;
  max-width: 440px;
  padding: 24px;
}

h1 {
  font-size: 14px;
  color: #4a2069;
  text-align: center;
  line-height: 1.6;
  margin-bottom: 24px;
}

.field {
  margin-bottom: 16px;
}

label {
  display: block;
  font-size: 10px;
  color: #5e2a87;
  margin-bottom: 8px;
  line-height: 1.4;
}

.notes-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 8px;
  margin-bottom: 16px;
}

input {
  width: 100%;
  padding: 10px 8px;
  font-size: 10px;
  background-color: #ffffff;
  color: #33154a;
  border: 3px solid #7b43a8;
  box-shadow: 2px 2px 0px #4a2069;
  outline: none;
}

input:focus {
  border-color: #9d4edd;
  background-color: #faf5ff;
}

button {
  width: 100%;
  padding: 14px;
  font-size: 11px;
  color: #ffffff;
  background-color: #8a4ba6;
  border: 3px solid #33154a;
  box-shadow: 4px 4px 0px #33154a;
  cursor: pointer;
  margin-top: 8px;
}

button:hover {
  background-color: #9d5bbd;
}

button:active {
  transform: translate(2px, 2px);
  box-shadow: 2px 2px 0px #33154a;
}

.resultado-box {
  margin-top: 20px;
  padding: 14px;
  font-size: 9px;
  line-height: 1.8;
  border: 3px dashed #7b43a8;
  background-color: #ebdcf9;
  display: none;
}

.status-aprovado {
  color: #1b5e20;
}

.status-reprovado {
  color: #b71c1c;
}
